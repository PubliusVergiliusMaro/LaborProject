<UserControl x:Class="LaborProjectOOP.Dekstop.Views.Pages.AddAuthorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LaborProjectOOP.Dekstop.Views.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1250">
    <Grid>
        <StackPanel Orientation="Vertical" Grid.Column="0" Margin="0,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label>Name</Label>
            <TextBox x:Name="nameAuthorTextBox" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="200"></TextBox>
            <Label>Surname</Label>
            <TextBox x:Name="surnameAuthorTextBox" Text="{Binding Surname, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Button Content="Add Author" Height="30" Command="{Binding AddAuthorCommand}" Margin="0 90 0 0"/>
        </StackPanel>
    </Grid>
</UserControl>
